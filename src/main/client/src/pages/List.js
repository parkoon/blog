import React from 'react';
import NavigationContainer from '../containers/NavigationContainer';

const List = () => {
  return (
    <div>
      <NavigationContainer />
    </div>
  );
};

export default List;
